# The "application" behind the proxy: a plain static responder with no TLS of
# its own. Its only job is to prove that traffic really travelled through the
# proxy rather than being answered by it.
{
	admin off
	auto_https off
	persist_config off
}

:8081 {
	respond "Hallo Welt von der Backend-Instanz"
}
